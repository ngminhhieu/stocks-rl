program: train.py
method: bayes
metric:
  name: reward
  goal: maximize
parameters:
  tp:
    distribution: int_uniform
    min: 5
    max: 20
  tn:
    distribution: int_uniform
    min: 0
    max: 5
  fp:
    distribution: int_uniform
    min: -5
    max: 0
  fn:
    distribution: int_uniform
    min: -5
    max: 0